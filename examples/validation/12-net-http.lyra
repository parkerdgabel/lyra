(*
  12-net-http.lyra — HTTP basics (requires network)
*)

url := "https://httpbin.org/get";
tmp := "target/tmp/http";
MakeDirectory[tmp, <|"Parents"->True|>];
dest := PathJoin[{tmp, "get.json"}];

HttpDownloadCached[url, dest, <|"TtlMs"->60000|>];
resp := HttpGet[url];
Part[resp, "status"];   
Part[resp, "body"];
