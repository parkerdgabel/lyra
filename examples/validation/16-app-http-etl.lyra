(*
  16-app-http-etl.lyra — Mini ETL: fetch JSON via HTTP, transform, write CSV
  Requires network. Writes output under target/tmp.
*)

tmp := "target/tmp/etl"; MakeDirectory[tmp, <|"parents"->True|>];
url := "https://httpbin.org/json";

resp := HttpGet[url];
data := Part[resp, "json"];  (* httpbin returns JSON; no branch needed *)

(* Project some fields if present; defensive Map *)
slides := Get[Get[data, "slideshow"], "slides"];
rows := Map[(<|"title"->Get[#, "title"], "type"->Get[#, "type"]|>)&, slides];
csv := RenderCSV[rows];
out := PathJoin[{tmp, "slides.csv"}];
csv >> out;
out;
