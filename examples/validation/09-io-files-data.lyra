(*
  09-io-files-data.lyra — File IO and data formats
  Creates a temp file in ./target/tmp (relative to repo) to avoid clutter.
*)

tmpdir := "target/tmp";
_ := Run["mkdir", "-p", tmpdir];

path := PathJoin[{tmpdir, "hello.txt"}];
"hello lyra" >> path;
<< path;                         (* => "hello lyra" *)

(* JSON/YAML/TOML parse/stringify *)
JsonStringify[<|a->1, b->2|>];
YamlParse["a: 1
b: 2
"];           (* => <|a->1, b->2|> *)
TomlParse["[x]
y=1
"];               (* => <|x-><|y->1|>|> *)
